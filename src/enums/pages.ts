export enum Pages {
  SUMMARY = "summary",
  PROJECTS = "projects",
  TASKS = "tasks",
  TICKETS = "tickets",
  EMPLOYEES = "employees",
  CHAT = "chat",
  ATTENDANCE = "attendance",
  CALENDAR = "calendar",
}
